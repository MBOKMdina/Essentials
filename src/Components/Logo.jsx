import { Link } from "react-router-dom"
export default function Logo(props)
{
    return(
        <div className={props.className}>
            <div className="k-container">K</div>
            -essentials
        </div>)
}